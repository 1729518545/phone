{
  "name": "customer-quick-contact",
  "version": "1.0.0",
  "description": "客户快捷联系 H5：识别/录入手机号 → 一键发送短信 / 打开微信 / 复制号码，预设模板支持后台配置",
  "private": true,
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "node scripts/build.js",
    "preview": "node -e \"const http=require('http'),fs=require('fs'),path=require('path');const ROOT=path.join(__dirname,'dist');if(!fs.existsSync(ROOT)){console.log('请先执行：npm run build');process.exit(1);}const PORT=process.env.PORT||8080;const MIME={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'application/javascript; charset=utf-8','.json':'application/json; charset=utf-8','.png':'image/png','.jpg':'image/jpeg','.svg':'image/svg+xml','.ico':'image/x-icon'};http.createServer((req,res)=>{let p=decodeURIComponent(req.url.split('?')[0]);if(p==='/')p='/index.html';const f=path.join(ROOT,p);if(!f.startsWith(ROOT)){res.writeHead(403);return res.end('Forbidden');}fs.readFile(f,(err,data)=>{if(err){res.writeHead(404);return res.end('Not Found: '+p);}res.writeHead(200,{'Content-Type':MIME[path.extname(f)]||'application/octet-stream'});res.end(data);});}).listen(PORT,()=>{console.log('');console.log('\\x1b[32m✅ dist/ 预览服务已启动\\x1b[0m');console.log('   主页: http://localhost:'+PORT+'/');console.log('   配置: http://localhost:'+PORT+'/config.html');console.log('   局域网手机访问: http://<本机IP>:'+PORT+'/');console.log('   Ctrl+C 停止');console.log('');});\"",
    "vercel:login": "npx.cmd vercel login",
    "deploy:preview": "npm run build && npx.cmd vercel --yes",
    "deploy": "npm run build && npx.cmd vercel --prod --yes",
    "deploy:drag-help": "echo 方式A最快：直接把 dist.zip 拖入 https://vercel.com/new 的 Upload 框即可上线"
  },
  "keywords": [
    "h5",
    "wechat",
    "sms",
    "customer-service",
    "ocr-phone"
  ],
  "engines": {
    "node": ">=14"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "tesseract.js": "^5.1.1"
  },
  "dependencies": {
    "tesseract.js-core": "^5.1.0"
  }
}
